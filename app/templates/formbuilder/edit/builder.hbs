{{page-title "Builder"}}

<AuBodyContainer
  class="formBuilderEdit__builder au-u-padding-small"
>
  <RdfForm
    @groupClass="au-o-grid__item"
    @form={{@model.form}}
    @graphs={{@model.graphs}}
    @formStore={{@model.formStore}}
    @forceShowErrors={{false}}
    @useNewListingLayout={{true}}
  />
</AuBodyContainer>