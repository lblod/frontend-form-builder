import Route from '@ember/routing/route';

export default class FormbuilderEditBuilderRoute extends Route {
  constructor() {
    super(...arguments);
    console.log(`Builder router constructor`);
  }
}
