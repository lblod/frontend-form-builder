import Route from '@ember/routing/route';

export default class FormbuilderEditValidationsRoute extends Route {
  constructor() {
    super(...arguments);
    console.log(`Validations router constructor`);
  }
}
