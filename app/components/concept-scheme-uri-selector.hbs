<AuLabel>{{t "conceptSchemeUriSelector.choose"}}</AuLabel>
<PowerSelect
  @options={{this.options}}
  @selected={{this.selected}}
  @onChange={{this.setSelected}}
  as |conceptScheme|
>
  {{conceptScheme.label}}
</PowerSelect>