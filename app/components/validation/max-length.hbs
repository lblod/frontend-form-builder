{{#if this.isForValidationType}}
  <AuLabel
    for={{this.inputId}}
    @required={{true}}
    @error={{this.isMaxLengthInputNotValid}}
  >
    {{t "validationTab.maxLengthLabel"}}
  </AuLabel>
  <AuInput
    @error={{this.isMaxLengthInputNotValid}}
    id={{this.inputId}}
    lang="nl-BE"
    type="number"
    value={{this.maxLength}}
    min={{0}}
    max={{9999}}
    {{on "blur" this.updateMaxLength}}
  />

  {{#if this.isMaxLengthInputNotValid}}
    <AuHelpText @error={{true}}>{{t
        "constraints.maxLengthMustBeGreaterThan"
        minCharacters=this.minCharacters
      }}</AuHelpText>
  {{/if}}
{{/if}}