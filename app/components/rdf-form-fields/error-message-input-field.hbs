<AuLabel
    for={{this.inputId}}
  >
    {{@field.label}}
</AuLabel>
<AuInput
  @error={{this.hasErrors}}
  @disabled={{@show}}
  @width={{if @inTable "block"}}
  id={{this.inputId}}
  value={{this.value}}
  {{on "blur" this.updateValue}}
/>